# Copy to .env.local for local development, or set as an environment
# variable in your hosting provider's dashboard for a deployed build.

# Base URL of the json-server mock API (see `npm run server`).
# Defaults to http://localhost:4000 when unset.
VITE_API_BASE_URL=http://localhost:4000
